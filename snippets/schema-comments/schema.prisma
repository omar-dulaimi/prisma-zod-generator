// Demonstrates rich @zod comments for extra validation (Issue #80)
// Adjust to your schema. Works with Zod v4 using the generator comment parser.

model User {
  id        String   @id @default(uuid())
  email     String   /// @zod.email().min(5, "too short").max(100, "too long")
  password  String   /// @zod.min(8, "min 8 chars")
  name      String?  /// @zod.trim()
  createdAt DateTime @default(now())
}
